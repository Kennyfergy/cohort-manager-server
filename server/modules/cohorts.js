const cohorts = [
  (cohortName = "Koss"),
  (cohortCount = 7),

  //anytime you send data across wire with urlencoded, which is what ajax uses and postman urlencoded uses, it comes through as a string
];
module.exports = cohorts;
//export out of this file
